[package]
name = "rtv"
version = "2.0.1"
edition = "2021"
authors = ["foxcirc"]
description = "Minimal HTTP/S client that supports nonblocking and streaming requests using mio."
keywords = ["http", "client", "simple", "nonblocking", "mio"]
categories = ["web-programming", "web-programming::http-client"]
repository = "https://github.com/Foxcirc/rtv"
readme = "readme.md"
license = "MIT"

[package.metadata.docs.rs]
all-features = true

[lib]
doctest = false

[features]
default = ["tls"]
tls = ["rustls", "webpki-roots"]

[dependencies]
dns-parser   = { version = "0.8.0" }
mio          = { version = "0.8.*", features = ["net", "os-poll"] }
rustls       = { version = "0.21.*", optional = true }
webpki-roots = { version = "0.23.*", optional = true }
chunked_transfer = { path = "dependencies/chunked-transfer" }
httparse = "1.8.0"
